import { Fragment } from "react"
import './Card.css';
import React from "react";

const Card: React.FC = (props) => {
    return (
        <Fragment>
        </Fragment>
    )
}

export default Card;